import React from "react";

const Button = () => {
  return (
    <button
      style={{
        "border-radius": "15px",
        padding: "10px 20px",
        "background-color": "#fff",
        cursor: "pointer",
        outline: "none"
      }}
    >
      Kirim
    </button>
  );
};

export default Button;
